server:
  port: 8081
spring:
  application:
    name: spring-boot-config-server
  datasource:
    url: jdbc:mysql://localhost:3306/his1222
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      properties:
        hibernate:
          dialect=org:
            hibernate:
              dialect:
                MySQL5Dialect: org.hibernate.dialect.MySQL5Dialect
